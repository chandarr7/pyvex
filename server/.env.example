# ─── Required ────────────────────────────────────────────────────────────────
# Clerk secret key. ROTATE the one that was pasted in chat before using.
# Get a fresh one from: https://dashboard.clerk.com → API keys
CLERK_SECRET_KEY=sk_test_REPLACE_ME_WITH_A_FRESH_SECRET

# Clerk publishable key — same one used by the frontend.
CLERK_PUBLISHABLE_KEY=pk_test_aW1tdW5lLWJpc29uLTE4LmNsZXJrLmFjY291bnRzLmRldiQ

# ─── Optional ────────────────────────────────────────────────────────────────
PORT=4000

# Comma-separated list of allowed origins for CORS. Add your production domain
# here when you deploy.
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:8080,http://127.0.0.1:5500
