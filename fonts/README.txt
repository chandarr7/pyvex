/*
  PYVEX — fonts are loaded via Google Fonts CDN in colors_and_type.css.
  No local woff2 files are bundled in this version of the system.

  If you have licensed alternatives (Söhne, GT America, NB International,
  Bodoni Moda), drop the .woff2 files in this folder and add @font-face
  declarations below, then remove the @import lines from colors_and_type.css.

  Current substitutions (Google Fonts → close visual matches):
    - Cormorant Garamond  (display serif)   — Prada-esque, italianate
    - Geist               (UI sans)          — Apple/Linear-esque
    - Geist Mono          (data mono)        — OpenAI-esque
*/
